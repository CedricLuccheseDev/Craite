Lis le fichier `.gitlab/merge_request_templates/Default.md` pour comprendre le format de description de MR attendu.

Ensuite, analyse les changements de la branche courante par rapport à main avec `git diff main...HEAD` et `git log main..HEAD --oneline`.

Génère une description de MR complète en français en remplissant chaque section du template selon les changements détectés.

Ne mets pas de mention "generated by" ou similaire.

Formate le résultat dans un bloc de code markdown (``` markdown ... ```) pour qu'il soit facilement copiable.
